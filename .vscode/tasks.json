{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CFS Scheduler",
            "type": "shell",
            "command": "cmake .. && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Configure and build the project using CMake and Make.",
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ]
}
